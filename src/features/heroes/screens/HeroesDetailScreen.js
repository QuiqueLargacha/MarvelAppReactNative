import React from "react";
import { View, Text, StyleSheet, Image } from "react-native";

const HeroesDetailScreen = () => {

    return (
        <View>
            <Text>Detail Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create ({});

export default HeroesDetailScreen;